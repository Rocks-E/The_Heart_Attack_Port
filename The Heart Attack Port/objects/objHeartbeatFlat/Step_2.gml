/// @description Insert description here
// You can write your code in this editor
/*
if (!paused){			
			//trace('heartbeatflat not paused');
	if (heartbeatDirection)
		x -= heartController.pulseSpeed;
	else
		x += heartController.pulseSpeed;
}
		*/
			// Off screen
//if(global.phase<6){		
/*
	if (self.heartController.heartbeatDirection=false and x > (room_width + sprite_width * 2)){
		offscreenAction();}
	if (self.heartController.heartbeatDirection and x < 0-sprite_width*5){
		offscreenAction();}
//}
			// Fade out?
	if (fading){
	//	image_alpha = fadeTween.alpha;
	}	
	